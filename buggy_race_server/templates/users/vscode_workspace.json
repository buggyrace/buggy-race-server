{
	"folders": [],
	"tasks": {
		"version": "2.0.0",
		"tasks": [
		  {
        "label": "{{ project_name }}: download from GitHub",
        "type": "shell",
        "command": "echo \"Enter the password for your account ({{ username }}) on {{ remote_server_name }}: \" && ssh {{ username }}@{{ remote_server_address }} \"git clone {{ github_repo }} buggy-editor\""
      }
		]
  },
	"remoteAuthority": "ssh-remote+{{ username }}@{{ remote_server_address }}"
}
